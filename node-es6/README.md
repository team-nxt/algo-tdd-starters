If you don't mind using Homebrew/Yarn, you may automatically install dependencies using:
```
./init.sh
```

Run tests with:
```
yarn test
```

Alternatively, manage dependencies yourself using `npm` (`package.lock` is included).

Have fun coding!
